import React from 'react';
import ReactDOM from 'react-dom';
import Header from '../__component__';

it('renders without crashing', () => {
  const div = document.createElement('div');
  ReactDOM.render(<__component__ />, div);
});
