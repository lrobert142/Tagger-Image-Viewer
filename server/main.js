import { Meteor } from 'meteor/meteor';
import { Images } from "../imports/api/images-collection.js"
import { Records } from '../imports/api/records-collection.js';

Meteor.startup(() => {
  // code to run on server at startup
});
